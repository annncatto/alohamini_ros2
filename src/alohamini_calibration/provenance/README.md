# Bootstrap provenance

The encoder/gripper mappings were initially consolidated from the current
RoboTwin asset copy, including the LeRobot MoveIt M4 hardware-test correction.
Camera candidates were initially consolidated from ManiSkill outputs.

These are historical import records only. `alohamini_calibration` is now the
authority; downstream copies do not overwrite it.
